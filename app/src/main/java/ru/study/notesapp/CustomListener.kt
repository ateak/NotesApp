package ru.study.notesapp

/**
 * Интерфейс содержит метод, который используется для просмотра подробного описания заметки в отдельной активити
 */
interface CustomListener {

    fun onClick(note : Note)
}